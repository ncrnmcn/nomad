views:
  - type: table
    name: Table
    filters:
      and:
        - file.path.contains("M01")
        - '!file.name.contains("base")'
        - '!file.name.contains("M01")'
    sort:
      - property: file.name
        direction: ASC
