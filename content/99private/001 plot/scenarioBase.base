views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder.startsWith("99private/001 plot")
        - '!file.name.contains("base")'
    order:
      - file.name
      - tags
    sort:
      - property: file.name
        direction: ASC
  - type: table
    name: View
